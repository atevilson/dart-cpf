name: "validador_cpf"

environment:
  sdk: '^3.4.0'
dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  meta: ^1.16.0
  test: ^1.25.8
dev_dependencies:
  coverage: ^1.11.1
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
